[build-system]
requires = ["setuptools>=42.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "encounter"
version = "4.18.1"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src"}
